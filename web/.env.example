# API base URL (no trailing slash). Use localhost when backend runs on same machine.
# If backend runs on another machine, use that IP e.g. http://192.168.1.10:3000
VITE_API_URL=http://localhost:3000
